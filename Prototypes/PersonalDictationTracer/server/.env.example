# Generate with: openssl rand -hex 32
HEX_PROXY_TOKEN=replace-with-a-random-64-character-hex-token

# Defaults shown for documentation; Compose supplies the upstream URL.
HEX_LISTEN_HOST=0.0.0.0
HEX_LISTEN_PORT=8787
